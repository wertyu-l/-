# 大屏模块设计文档

## 1. 概述

### 1.1 模块定位

大屏模块是异构硬件设备管控系统的**配置**，负责拼接大屏的创建、设备通道绑定。

**核心职责：**
- 创建拼接大屏（定义行列数，自动生成屏幕单元）
- 将设备输出通道绑定到屏幕单元

---

## 2. 模块结构

### 2.1 文件结构

大屏模块位于 `demo1-server` 中：

```text
demo1-server/src/main/java/com/example/demo/
├── controller/
│   └── ScreenController.java          ← 新增：大屏配置接口层
├── service/
│   ├── ScreenService.java             ← 新增：大屏业务接口
│   └── impl/
│       └── ScreenServiceImpl.java     ← 新增：大屏配置业务实现
├── mapper/
│   └── ScreenMapper.java              ← 新增：大屏 + 单元数据访问
├── common/
│   ├── ScreenPageDTO.java             ← 新增：分页查询 DTO
│   ├── ScreenPageVO.java              ← 新增：分页返回 VO
│   ├── ScreenDetailVO.java            ← 新增：大屏详情 VO
│   ├── CellVO.java                    ← 新增：单元 VO
│   ├── ScreenCreateRequest.java       ← 新增：创建大屏请求
│   ├── CellBindRequest.java           ← 新增：单元绑定请求
└── driver/
    ├── DeviceDriver.java              ← 已有
    ├── RestDeviceDriver.java          ← 已有（用于调用模拟设备窗口 API）
    └── DeviceEndpoint.java            ← 已有
```

### 2.2 依赖关系

```text
ScreenController
    └── ScreenService
            ├── ScreenMapper          → 数据库 CRUD（大屏、单元）
            ├── DeviceMapper          → 查设备信息（baseUrl、在线状态）
            └── RestTemplate          → 调用模拟设备窗口 API（转发）
```

---

## 3. 数据库设计

### 3.1 建表 SQL

在 `demo1-server/src/main/resources/schema.sql` 中追加：

```sql
-- =============================================
-- 大屏配置模块
-- =============================================
CREATE TABLE IF NOT EXISTS SCREEN (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    screen_name     VARCHAR(200)  NOT NULL,
    rows_count      INT           NOT NULL DEFAULT 1,
    cols_count      INT           NOT NULL DEFAULT 1,
    cell_width       INT           NOT NULL DEFAULT 1920,
    cell_height      INT           NOT NULL DEFAULT 1080,
    create_time     TIMESTAMP     DEFAULT CURRENT_TIMESTAMP,
    update_time     TIMESTAMP     DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS SCREEN_CELL (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    screen_id       BIGINT        NOT NULL,
    row_index       INT           NOT NULL DEFAULT 0,
    col_index       INT           NOT NULL DEFAULT 0,
    device_id       BIGINT        NULL,
    channel         INT           NOT NULL DEFAULT 1,
    CONSTRAINT fk_cell_screen FOREIGN KEY (screen_id) REFERENCES SCREEN(id) ON DELETE CASCADE
);

```

### 3.2 表关系图

```text
SCREEN (大屏)
  id ──────────────────────────────┐
  screen_name                      │
  rows_count                       │
  cols_count                       │
  cell_width                       │
  cell_height                      │
                                   │
SCREEN_CELL (屏幕单元)              │
  id                               │
  screen_id ───────────────────────┤ 外键 CASCADE
  row_index                        │
  col_index                        │
  device_id ──→ DEVICE.id (可为NULL)
  channel                          │
                                   │
SCREEN_WINDOW (窗口，由前台模块管理)   │
  id                               │
  window_id                        │
  screen_id ───────────────────────┤ 外键 CASCADE
  device_id ──→ DEVICE.id          │
  channel                          │
  x, y, width, height              │
  source_type, source_url          │
  sync_status ← `synced` | `pending` | `failed`
```

### 3.3 字段说明

**SCREEN（大屏表）**

| 字段 | 类型 | 必填 | 说明 |
|------|------|:--:|------|
| id | BIGINT | 是 | 主键，自增 |
| screen_name | VARCHAR(200) | 是 | 大屏名称 |
| rows_count | INT | 是 | 行数，默认 1 |
| cols_count | INT | 是 | 列数，默认 1 |
| cell_width | INT | 是 | 单元宽度（px），默认 1920。绑定设备时校验设备分辨率必须匹配 |
| cell_height | INT | 是 | 单元高度（px），默认 1080。绑定设备时校验设备分辨率必须匹配 |

**SCREEN_CELL（屏幕单元表）**

| 字段 | 类型 | 必填 | 说明 |
|------|------|:--:|------|
| id | BIGINT | 是 | 主键，自增 |
| screen_id | BIGINT | 是 | 关联大屏，级联删除 |
| row_index | INT | 是 | 行索引，从 0 开始 |
| col_index | INT | 是 | 列索引，从 0 开始 |
| device_id | BIGINT | 否 | 绑定的设备 ID，NULL=未绑定 |
| channel | INT | 是 | 通道编号，默认 1 |


---

## 4. 数据模型

### 4.1 Screen — 大屏实体

位于 `demo1-server` 的 `entity/` 包下，对应 `SCREEN` 表：

```java
@Data
public class Screen {
    private Long id;               // 主键，自增
    private String screenName;     // 大屏名称
    private Integer rowsCount;     // 行数，默认 1
    private Integer colsCount;     // 列数
    private Integer cellWidth;      // 单元宽度（px），默认 1920
    private Integer cellHeight;     // 单元高度（px），默认 1080
    private LocalDateTime createTime;
    private LocalDateTime updateTime;
}
```

### 4.2 ScreenCell — 单元实体

位于 `demo1-server` 的 `entity/` 包下，对应 `SCREEN_CELL` 表：

```java
@Data
public class ScreenCell {
    private Long id;               // 主键，自增
    private Long screenId;         // 关联大屏
    private Integer rowIndex;      // 行索引，从 0 开始
    private Integer colIndex;      // 列索引，从 0 开始
    private Long deviceId;         // 绑定设备 ID，NULL=未绑定
    private Integer channel;       // 通道编号，默认 1
}
```

### 4.3 ScreenPageVO — 大屏列表 VO

放在 `demo1-server` 的 `common/` 包下：

```java
@Data
public class ScreenPageVO {
    private Long id;               // 大屏 ID
    private String screenName;     // 大屏名称
    private Integer rowsCount;     // 行数
    private Integer colsCount;     // 列数
    private Integer cellWidth;      // 单元宽度（px）
    private Integer cellHeight;     // 单元高度（px）
    private Integer cellCount;     // 单元数量
    private Integer windowCount;   // 窗口数量（由前台模块查询）
    private String createTime;
}
```

### 4.4 ScreenDetailVO — 大屏详情 VO

放在 `demo1-server` 的 `common/` 包下：

```java
@Data
public class ScreenDetailVO {
    private Long id;                     // 大屏 ID
    private String screenName;           // 大屏名称
    private Integer rowsCount;           // 行数
    private Integer colsCount;           // 列数
    private Integer cellWidth;            // 单元宽度（px）
    private Integer cellHeight;           // 单元高度（px）
    private List<CellVO> cells;          // 单元列表
    private List<ScreenWindowVO> windows; // 窗口列表（由前台模块查询）
    private String createTime;
}
```

### 4.5 CellVO — 单元返回 VO

放在 `demo1-server` 的 `common/` 包下：

```java
@Data
public class CellVO {
    private Long id;               // 单元 ID
    private Long screenId;         // 所属大屏
    private Integer rowIndex;      // 行索引
    private Integer colIndex;      // 列索引
    private Long deviceId;         // 绑定设备 ID
    private Integer channel;       // 通道编号
    private String deviceName;     // 设备名称
    private String deviceType;     // 设备类型
    private Integer online;        // 在线状态（1=在线，0=离线）
    private String baseUrl;        // 设备地址（前台信号源需要）
    private Integer outputChannels;// 输出通道数
}
```

### 4.6 ScreenCreateRequest — 创建大屏请求 DTO

放在 `demo1-server` 的 `common/` 包下：

```java
@Data
public class ScreenCreateRequest {
    private String screenName;     // 大屏名称
    private Integer rowsCount;     // 行数，默认 1
    private Integer colsCount;     // 列数
    private Integer cellWidth;      // 单元宽度（px），默认 1920
    private Integer cellHeight;     // 单元高度（px），默认 1080
}
```

### 4.7 CellBindRequest — 单元绑定设备请求 DTO

放在 `demo1-server` 的 `common/` 包下：

```java
@Data
public class CellBindRequest {
    private Long deviceId;         // 设备 ID
    private Integer channel;       // 通道编号，默认 1
}
```
## 5. 接口

### 5.1 接口汇总

| 方法 | 路径 | 用途 | 说明 |
|------|------|------|------|
| POST | `/screen` | 创建大屏 | 自动生成 rows×cols 个单元 |
| GET | `/screen/page` | 分页查询大屏列表 | 含单元数和窗口数 |
| GET | `/screen/{id}` | 获取大屏详情 | 含单元列表 + 窗口列表 |
| DELETE | `/screen/{id}` | 删除大屏 | 级联删除单元和窗口 |
| PUT | `/screen/{screenId}/cell/{cellId}` | 绑定设备通道 | 查 DEVICE 表校验设备信息及能力后绑定 |
| DELETE | `/screen/{screenId}/cell/{cellId}` | 解除绑定 | 设 device_id=NULL |

### 5.2 接口详情

#### 5.2.1 创建大屏

```
POST /screen
Content-Type: application/json
```

**请求体：**

```json
{
    "screenName": "演示大屏1",
    "rowsCount": 2,
    "colsCount": 2,
    "cellWidth": 1920,
    "cellHeight": 1080
}
```

**成功响应：**

```json
{
    "code": 1,
    "msg": null,
    "data": {
        "id": 1,
        "screenName": "演示大屏1",
        "rowsCount": 2,
        "colsCount": 2,
        "cellWidth": 1920,
        "cellHeight": 1080,
        "cells": [
            { "id": 1, "rowIndex": 0, "colIndex": 0, "deviceId": null },
            { "id": 2, "rowIndex": 0, "colIndex": 1, "deviceId": null },
            { "id": 3, "rowIndex": 1, "colIndex": 0, "deviceId": null },
            { "id": 4, "rowIndex": 1, "colIndex": 1, "deviceId": null }
        ]
    }
}
```

#### 5.2.2 分页查询大屏列表

```
GET /screen/page?page=1&size=10&keyword=演示
```

**成功响应：**

```json
{
    "code": 1,
    "msg": null,
    "data": {
        "total": 1,
        "list": [
            {
                "id": 1,
                "screenName": "演示大屏1",
                "rowsCount": 2,
                "colsCount": 2,
                "cellWidth": 1920,
                "cellHeight": 1080,
                "cellCount": 4,
                "windowCount": 0
            }
        ]
    }
}
```

#### 5.2.3 获取大屏详情

```
GET /screen/{id}
```

**成功响应：**

```json
{
    "code": 1,
    "msg": null,
    "data": {
        "id": 1,
        "screenName": "演示大屏1",
        "rowsCount": 2,
        "colsCount": 2,
        "cellWidth": 1920,
        "cellHeight": 1080,
        "cells": [
            { "id": 1, "rowIndex": 0, "colIndex": 0, "deviceId": 1, "deviceName": "REST-Node-01", "deviceBaseUrl": "http://192.168.1.100:8086", "channel": 1 },
            { "id": 2, "rowIndex": 0, "colIndex": 1, "deviceId": null, "deviceName": null, "deviceBaseUrl": null, "channel": null },
            { "id": 3, "rowIndex": 1, "colIndex": 0, "deviceId": null, "deviceName": null, "deviceBaseUrl": null, "channel": null },
            { "id": 4, "rowIndex": 1, "colIndex": 1, "deviceId": null, "deviceName": null, "deviceBaseUrl": null, "channel": null }
        ],
        "windows": []
    }
}
```

#### 5.2.4 删除大屏

```
DELETE /screen/{id}
```

**成功响应：**

```json
{
    "code": 1,
    "msg": null,
    "data": null
}
```

#### 5.2.5 绑定设备通道

```
PUT /screen/{screenId}/cell/{cellId}
Content-Type: application/json
```

**请求体：**

```json
{
    "deviceId": 1,
    "channel": 1
}
```

**成功响应：**

```json
{
    "code": 1,
    "msg": null,
    "data": {
        "id": 1,
        "rowIndex": 0,
        "colIndex": 0,
        "deviceId": 1,
        "deviceName": "REST-Node-01",
        "channel": 1
    }
}
```

**失败响应（设备不存在）：**

```json
{
    "code": 0,
    "msg": "设备不存在: 999",
    "data": null
}
```

**失败响应（设备离线）：**

```json
{
    "code": 0,
    "msg": "设备 REST-Node-01 当前离线，不允许绑定",
    "data": null
}
```

**失败响应（设备分辨率不匹配）：**

```json
{
    "code": 0,
    "msg": "设备分辨率 3840x2160 不匹配大屏单元分辨率 1920x1080",
    "data": null
}
```

**失败响应（通道总数超限）：**

```json
{
    "code": 0,
    "msg": "该设备通道总数已达上限（output_channels 已满）",
    "data": null
}
```

#### 5.2.6 解除绑定

```
DELETE /screen/{screenId}/cell/{cellId}
```

**成功响应：**

```json
{
    "code": 1,
    "msg": null,
    "data": null
}
```


---


## 6. 要完成的功能（流程图）

### 6.1 大屏配置流程

```text
POST /screen 创建大屏 (rows×cols)
  └── 自动生成 SCREEN_CELL 单元

PUT /screen/{id}/cell/{cellId} 绑定设备通道
  ├── 校验：查 DEVICE 表确认设备存在、在线
  ├── 校验：统计所有大屏中该设备已绑定总数，确保不超过 output_channels
  ├── 校验：查 DEVICE 表获取设备能力字段（maxWindows / supportMove / supportResize / supportOverlay），返回供前台使用
  └── 更新 SCREEN_CELL.device_id

DELETE /screen/{id}/cell/{cellId} 解除绑定
  └── 设 device_id=NULL

DELETE /screen/{id} 删除大屏
  └── CASCADE 删除 SCREEN_CELL 和 SCREEN_WINDOW
```


### 6.2 窗口状态恢复

```text
GET /screen/{id}/windows
  └── 从 SCREEN_WINDOW 表查询
       ├── synced → 正常返回
       └── pending → 尝试重新同步到模拟设备
```

### 6.3 整体业务闭环

```text
用户登录 → 添加设备 → 创建大屏 → 绑定设备输出通道到单元
  → 进入前台 → 拖拽输入通道到画布 → 创建窗口 → 后台双写 → 前台渲染交互
```

---

## 7. 机制和要注意的点

### 7.1 单元绑定校验规则

| 校验项 | 说明 |
|------|------|
| 设备存在 | 查 DEVICE 表确认 id 存在 |
| 设备已启用 | `enabled=1` |
| 设备在线 | `online=1`，离线设备不允许绑定 |
| 通道总数不超限 | 所有大屏中该设备的绑定总数 ≤ `output_channels`（查 DEVICE 表 output_channels 字段） |
| 分辨率匹配 | 设备 `maxResolution` 与单元 `cellWidth x cellHeight` 匹配（查 DEVICE 表 max_resolution 字段） |
| 设备能力 | 查 DEVICE 表获取能力字段（maxWindows / supportMove / supportResize / supportOverlay），返回供前台约束窗口交互 |

### 7.2 事务保证

使用 `@Transactional` 保证：
- 创建大屏时 SCREEN 和 SCREEN_CELL 写入的原子性
- 删除大屏时级联删除的原子性
